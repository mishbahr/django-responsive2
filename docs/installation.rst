============
Installation
============

At the command line::

    $ easy_install django-responsive2

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-responsive2
    $ pip install django-responsive2